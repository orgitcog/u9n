@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Threads)
find_dependency(Eigen3 QUIET)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/un9nTargets.cmake")

# Check required components
check_required_components(un9n)

# Provide version information
set(un9n_VERSION @PROJECT_VERSION@)
set(un9n_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(un9n_VERSION_MINOR @PROJECT_VERSION_MINOR@)
set(un9n_VERSION_PATCH @PROJECT_VERSION_PATCH@)
